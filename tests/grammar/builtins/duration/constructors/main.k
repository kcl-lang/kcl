import duration

s = duration.second(45)
m = duration.minute(30)
h = duration.hour(1)
d = duration.day(2)
zero = duration.second(0)
frac = duration.minute(1.5)
combined = duration.hour(1.5)
big = duration.day(1.5)
overflow = duration.hour(24)
