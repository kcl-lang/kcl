# Fixture-side git repository, created by tests/grammar/conftest.py around
# the pytest session. See read_git_ref/git_setup.yaml for the format.
file: asset.txt
revisions:
  - content: |
      version one
